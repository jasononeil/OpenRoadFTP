-js includes/js/ajaxclient.js
-main AjaxClient
-cp src/classes/
-cp src/
-lib feffects
# HttpAsyncConnection is used by JS that isn't compiled from Haxe, so
# we need to explicitly keep it from being removed by DCE
--macro keep('haxe.remoting.HttpAsyncConnection')

