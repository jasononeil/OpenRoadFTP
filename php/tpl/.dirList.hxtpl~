<div class="dirList" title="{dir.path}">
<h4><span>{dir.name}</span></h4>
<ul>
	<hxLoop name="ftpItem">
	<li>
		<div class="label">{file.name}</div>
		<span class="name">{file.name}</span>
		<span class="path">{file.path}</span>
		<span class="type">{file.type}</span>
		<span class="size">{file.size}</span>
		<span class="owner">{file.owner}</span>
		<span class="group">{file.group}</span>
		<span class="modified">{file.modified}</span>
		<span class="permissions">{file.permissions}</span>
	</li>
	</hxLoop>
</ul>
<div class="dirListSpacer"> </div>
</div>